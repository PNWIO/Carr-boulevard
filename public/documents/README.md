# public/documents

Ungated PDFs only — anything here is downloadable by anyone who knows the URL.

Gated due-diligence documents belong in the R2 bucket bound as `DOCS`, and are served
through `functions/documents/[[path]].ts` after the access cookie is verified. Putting them
here would make the data-room gate decorative.
